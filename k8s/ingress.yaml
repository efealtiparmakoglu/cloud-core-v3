apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cloud-core-v3-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: cloud-core-v3.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: cloud-core-v3-service
            port:
              number: 80
